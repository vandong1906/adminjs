const products = require('./products');
const collections = require('./collections');
const { customers, customerGroups } = require('./customers');
const orders = require('./orders');

module.exports = {
  products,
  collections,
  customers,
  customerGroups,
  orders
}; 